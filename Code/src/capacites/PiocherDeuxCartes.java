package capacites;

import model.game.GameManager;
import model.player.Player;

public class PiocherDeuxCartes extends CapaciteSpeciale {

    // Vous piochez deux cartes au hasard dans la main d'une autre divinite
    
    public PiocherDeuxCartes(){}
    @Override
    public void effectuerCapaciteSpeciale(Player player, GameManager gameManager) {
    }
}
